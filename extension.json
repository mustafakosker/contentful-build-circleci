{
  "id": "build-circleci",
  "name": "CircleCI - Build Manually",
  "srcdoc": "./index.html",
  "fieldTypes": ["Symbol"],
  "sidebar": true,
  "parameters": {
    "instance": [
      {
        "id": "circleciApiToken",
        "type": "Symbol",
        "name": "CircleCI User Access Token",
        "description": "User Access token to build and fetch statuses from CircleCI",
        "required": true
      },
      {
        "id": "access",
        "type": "Enum",
        "name": "Contentful user access",
        "description": "Allow  triggering builds for all users or only for admins.",
        "options": [{ "all": "All users" }, { "admins": "Admins only" }],
        "required": true,
        "default": "all"
      }
    ]
  }
}
